# 推荐系统演示平台 · 部署说明

本项目是一个基于 Flask 的推荐系统演示网页，集成了两个图神经网络推荐模型：

| 模型 | 任务 | 可运行数据集 |
|------|------|--------------|
| **STSECL** | 朋友推荐 | BER、CHI、NYC、JK、KL、SP |
| **POIRec** | 兴趣点（POI）推荐 | BER、CHI |

网页通过后台子进程运行各模型的 `main.py`，训练完成后把结果存入 SQLite / TSV，再由前端页面展示。POIRec 的结果还会在 **Leaflet 地图**上标出目标用户（红点，居中）与推荐地点（蓝点）。

---

## 1. 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows / macOS / Linux 均可 |
| Python | **3.8**（本项目在 3.8.8 下开发验证，推荐使用 3.8） |
| 内存 / 显卡 | 训练需要较多内存；有 NVIDIA GPU 会更快，无 GPU 也可用 CPU 运行 |
| 网络 | 首次安装依赖需联网；地图（Leaflet + OpenStreetMap 瓦片）**运行期需联网**，离线时仅地图不显示，其余功能正常 |

> ⚠️ 依赖里最关键的是 **PyTorch 1.12.0 + torch-scatter + torch-geometric 2.2.0** 三者的版本必须严格匹配，不能随意用 `pip install torch` 装最新版。

---

## 2. 目录结构

```
RecommendationWeb/
├── app.py                    # Flask 后端入口（网页服务）
├── requirements.txt          # 基础依赖清单
├── templates/
│   └── index.html            # 单页前端
├── static/
│   ├── app.js                # 前端逻辑（含地图渲染）
│   └── style.css             # 样式
├── results/                  # 每次训练任务的日志（可清理，不必拷贝）
├── STSECL/                   # 朋友推荐模型
│   ├── main.py               # 训练 + 生成推荐（入口）
│   ├── config.py / preprocess.py / dataset.py / model.py / utils.py / metapath.py
│   └── data/
│       ├── BER/ CHI/ NYC/ JK/ KL/ SP/     # 各城市数据（已预处理为 .pkl / .pt）
│       └── save_user_embedding/           # 已训练好的模型权重 .pt
└── POIRec/                   # 兴趣点推荐模型
    ├── main.py               # 训练 + 生成推荐（入口）
    ├── config.py / preprocess.py / dataset.py / model.py / utils.py / metapath.py
    └── data/
        ├── BER/ CHI/                      # 各城市数据（含原始 CSV + poi_data.pt）
        └── save_user_embedding/           # 已训练好的模型权重 .pt
```

**拷贝到新电脑时，请完整复制整个项目文件夹（尤其是两个 `data/` 目录和 `save_user_embedding/` 权重）。**
- `STSECL/data` 里的数据是已经预处理好的 `.pkl` / `.pt`，**没有原始 CSV**，不能重新生成，必须整体复制。
- `POIRec/data` 里既有原始 CSV，也有预处理好的 `poi_data.pt`，可以直接用，也可以删掉缓存重新生成。
- `results/` 和 `__pycache__/` 是运行产物，可删除、可不拷贝。

> **从 GitHub 仓库 clone 的情况**：仓库通过 `.gitignore` 排除了 `*.pt`、`save_user_embedding/`、`*.db`、`recommendations.tsv` 等可再生成产物。因此 clone 后首次运行会自动重新预处理并训练；上面的"完整复制"只针对从本机原目录直接拷贝的场景。

---

## 3. 安装依赖

在项目根目录打开命令行（Windows 建议用 Anaconda Prompt），依次执行：

### 3.1 创建虚拟环境（推荐）

```bash
conda create -n recweb python=3.8 -y
conda activate recweb
```

### 3.2 安装基础依赖

```bash
pip install -r requirements.txt
```

即：`flask>=3.0`、`numpy`、`pandas`、`scipy`、`scikit-learn`。

### 3.3 安装 PyTorch 及图神经网络库（关键，版本必须匹配）

**有 NVIDIA 显卡（CUDA 11.6）时：**

```bash
pip install torch==1.12.0+cu116 --extra-index-url https://download.pytorch.org/whl/cu116
pip install torch-scatter==2.1.0 -f https://data.pyg.org/whl/torch-1.12.0+cu116.html
pip install torch-geometric==2.2.0
```

**无显卡 / 用 CPU 时：**

```bash
pip install torch==1.12.0+cpu -f https://download.pytorch.org/whl/torch_stable.html
pip install torch-scatter==2.1.0 -f https://data.pyg.org/whl/torch-1.12.0+cpu.html
pip install torch-geometric==2.2.0
```

> 说明：`torch-scatter` 是 C 扩展包，其编译好的 wheel 与 torch 版本一一对应，所以必须从 `data.pyg.org` 按 `torch-1.12.0+...` 的地址安装，不能用普通 `pip install torch-scatter`。

### 3.4 验证安装

```bash
python -c "import torch, torch_scatter, torch_geometric; print(torch.__version__)"
```

应能正常打印出 `1.12.0...` 且不报错。

---

## 4. 启动网页

在项目根目录执行：

```bash
python app.py
```

看到类似输出即启动成功：

```
 * Running on http://127.0.0.1:5000
```

用浏览器打开 **http://127.0.0.1:5000/** 即可。

> 默认端口 5000。若被占用，可修改 `app.py` 末尾的 `app.run(..., port=5000)` 改为其它端口。

---

## 5. 网页使用步骤

1. **任务选择**：点击「朋友推荐 (STSECL)」或「兴趣点推荐 (POIRec)」。
2. **数据集与参数**：下拉选择数据集，按需调整阈值参数（δ1/δ2 等），点「推荐」按钮。
   - 点「推荐」后，后端会在后台子进程里运行对应模型的 `main.py` 开始训练（模型收敛后自动早停），页面会轮询直到完成。
   - 训练耗时视数据量与机器性能而定，一般几分钟。
3. **查看结果**：完成后在「用户」框输入用户序号（0 开始的序号，即结果表格里 `user` 列的数字），填「推荐前 N 个」，点「查询」。
   - **POIRec** 会额外显示地图：红色圆点为目标用户（居中），蓝色圆点为推荐地点，点圆点可看地点类别 / POI 编号 / 评分。

> 同一时刻只允许一个训练任务在跑；若想换任务/参数，等当前任务结束，或刷新页面（刷新会自动中断当前任务）。


